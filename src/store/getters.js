const ingredientList = (state) => state.ingredientList;

const mealsByIngredient = (state) => state.mealsByIngredient;

const mealsByLetter = (state) => state.mealsByLetter;

const searchedMeals = (state) => state.searchedMeals;

export {
	ingredientList,
	mealsByIngredient,
	mealsByLetter,
	searchedMeals
};
